#include<stdio.h>
#include<conio.h>
int main()
{
    int a,b;
    printf("enter number a,b");
    scanf("%d %d",&a,&b);
    if (a>=b)
    printf("%d is the largest number",a);
    if (b>=a)
    printf("%d is the largest number",b);
    return 0;
}